# ============================================================
# MCP Server Environment Variables
# Copy this file to .env and fill in your values.
# ============================================================

# --- Log Analyzer with CloudWatch Logs (cw-log-analyzer) ---
# --- AWS Infrastructure as Code (aws-iac) ---
# --- Amazon ECS (ecs-mcp) ---
# AWS_DEFAULT_REGION=
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=

# --- PostgreSQL MCP Pro (pg-postgres-mcp-ro, pg-postgres-mcp-rw) ---
# PG_USER=
# PG_PASSWORD=
# PG_HOST=
# PG_PORT=
# PG_DATABASE=

# --- Google Sheets (google-sheets) ---
# GOOGLE_SHEETS_CREDENTIALS_CONFIG=
# GOOGLE_SHEETS_DRIVE_FOLDER_ID=

# --- Playwright Stealth (playwright-stealth) ---
# (no required env vars — defaults work out of the box)
# PROXY_URL=
# PROXY_USERNAME=
# PROXY_PASSWORD=

# --- DynamoDB (pulsemcp-dynamodb) ---
# (uses same AWS credentials as above)
# DYNAMODB_ENDPOINT=
# DYNAMODB_ENABLED_TOOL_GROUPS=readonly,readwrite
# DYNAMODB_ALLOWED_TABLES=

# --- AWS S3 (pulsemcp-s3) ---
# (uses same AWS credentials as above)
# AWS_ENDPOINT_URL=
# S3_BUCKET=
# S3_ENABLED_TOOLGROUPS=readonly,readwrite
# S3_ENABLED_TOOLS=
# S3_DISABLED_TOOLS=

# --- Chrome DevTools (chrome-devtools-remote, chrome-devtools-headless) ---
# (no required env vars — controls a local Chrome browser)
# CHROME_DEVTOOLS_MCP_NO_USAGE_STATISTICS=

# --- Neon Postgres (neon-remote-ro, neon-remote-rw) ---
# NEON_API_KEY=

# --- Langfuse (langfuse) ---
# LANGFUSE_API_CREDENTIALS=
# LANGFUSE_HOST=cloud.langfuse.com

# --- Vercel (vercel-ro, vercel-rw) ---
# VERCEL_TOKEN=
# VERCEL_TEAM_ID=
# VERCEL_TEAM_SLUG=

# --- Langfuse Observability (langfuse-observability) ---
# LANGFUSE_SECRET_KEY=
# LANGFUSE_PUBLIC_KEY=
# LANGFUSE_BASE_URL=https://cloud.langfuse.com
