{
  "cw-log-analyzer": {
    "title": "Log Analyzer with CloudWatch Logs",
    "description": "Use when debugging or investigating issues that would likely benefit from CloudWatch log introspection. Search and filter CloudWatch Logs, find error patterns, and correlate log events across multiple services.",
    "type": "stdio",
    "command": "uvx",
    "args": [
      "--from",
      "git+https://github.com/awslabs/Log-Analyzer-with-MCP",
      "cw-mcp-server"
    ],
    "env": {
      "AWS_DEFAULT_REGION": "us-east-1",
      "AWS_ACCESS_KEY_ID": "${AWS_ACCESS_KEY_ID}",
      "AWS_SECRET_ACCESS_KEY": "${AWS_SECRET_ACCESS_KEY}"
    }
  },
  "pg-postgres-mcp-ro": {
    "title": "PostgreSQL MCP Pro (Read-Only)",
    "description": "PostgreSQL tuning and analysis server. Provides index tuning, explain plans, health checks, and safe SQL execution. Read-only with resource limits.",
    "type": "stdio",
    "command": "uvx",
    "args": [
      "--from",
      "postgres-mcp==0.3.0",
      "postgres-mcp",
      "--access-mode=restricted"
    ],
    "env": {
      "DATABASE_URI": "postgresql://${PG_USER}:${PG_PASSWORD}@${PG_HOST}:${PG_PORT}/${PG_DATABASE}"
    }
  },
  "pg-postgres-mcp-rw": {
    "title": "PostgreSQL MCP Pro (Read-Write)",
    "description": "PostgreSQL tuning and analysis server. Provides index tuning, explain plans, health checks, and safe SQL execution. Full read/write access for development.",
    "type": "stdio",
    "command": "uvx",
    "args": [
      "--from",
      "postgres-mcp==0.3.0",
      "postgres-mcp",
      "--access-mode=unrestricted"
    ],
    "env": {
      "DATABASE_URI": "postgresql://${PG_USER}:${PG_PASSWORD}@${PG_HOST}:${PG_PORT}/${PG_DATABASE}"
    }
  },
  "aws-iac": {
    "title": "AWS Infrastructure as Code",
    "description": "Use when building or troubleshooting AWS infrastructure as code. Validate CloudFormation templates, check compliance, search CDK documentation, get best practices guidance, and debug deployment failures.",
    "type": "stdio",
    "command": "uvx",
    "args": [
      "awslabs.aws-iac-mcp-server==1.0.11"
    ],
    "env": {
      "AWS_PROFILE": "${AWS_PROFILE}",
      "AWS_DEFAULT_REGION": "${AWS_DEFAULT_REGION}",
      "AWS_ACCESS_KEY_ID": "${AWS_ACCESS_KEY_ID}",
      "AWS_SECRET_ACCESS_KEY": "${AWS_SECRET_ACCESS_KEY}",
      "AWS_SESSION_TOKEN": "${AWS_SESSION_TOKEN}",
      "FASTMCP_LOG_LEVEL": "ERROR"
    }
  },
  "google-sheets": {
    "title": "Google Sheets",
    "description": "Use when you need to read, create, or modify Google Sheets spreadsheets. Search for spreadsheets, read/write cell data, manage sheet tabs, add rows/columns, apply batch updates, share spreadsheets, and browse Drive folders.",
    "type": "stdio",
    "command": "uvx",
    "args": [
      "mcp-google-sheets==0.6.0"
    ],
    "env": {
      "CREDENTIALS_CONFIG": "${GOOGLE_SHEETS_CREDENTIALS_CONFIG}",
      "DRIVE_FOLDER_ID": "${GOOGLE_SHEETS_DRIVE_FOLDER_ID}"
    }
  },
  "pulsemcp-playwright": {
    "title": "Playwright",
    "description": "Use when you need to automate browser interactions or scrape pages. Execute Playwright code, take screenshots, and manage persistent browser sessions with optional stealth mode.",
    "type": "stdio",
    "command": "npx",
    "args": [
      "-y",
      "playwright-stealth-mcp-server@0.0.9"
    ],
    "env": {
      "STEALTH_MODE": "false",
      "HEADLESS": "true"
    }
  },
  "ecs-mcp": {
    "title": "Amazon ECS",
    "description": "Use when monitoring or troubleshooting ECS deployments. Check deployment status, fetch service events and task failures, pull task logs, detect image pull failures, and inspect network configuration.",
    "type": "stdio",
    "command": "uvx",
    "args": [
      "mcp-proxy-for-aws==1.1.6",
      "https://ecs-mcp.us-east-1.api.aws/mcp",
      "--service", "ecs-mcp",
      "--region", "us-east-1"
    ],
    "env": {
      "AWS_DEFAULT_REGION": "${AWS_DEFAULT_REGION}",
      "AWS_ACCESS_KEY_ID": "${AWS_ACCESS_KEY_ID}",
      "AWS_SECRET_ACCESS_KEY": "${AWS_SECRET_ACCESS_KEY}"
    }
  },
  "pulsemcp-dynamodb-ro": {
    "title": "DynamoDB (Read-Only)",
    "description": "Use when you need to inspect DynamoDB tables and data without modifying anything. List and describe tables, get items, query, scan, and batch get.",
    "type": "stdio",
    "command": "npx",
    "args": ["-y", "aws-dynamodb-mcp-server@0.2.0"],
    "env": {
      "AWS_REGION": "us-east-1",
      "AWS_ACCESS_KEY_ID": "${AWS_ACCESS_KEY_ID}",
      "AWS_SECRET_ACCESS_KEY": "${AWS_SECRET_ACCESS_KEY}",
      "DYNAMODB_ENABLED_TOOL_GROUPS": "readonly"
    }
  },
  "pulsemcp-dynamodb-rw": {
    "title": "DynamoDB (Read-Write)",
    "description": "Use when you need to read and write DynamoDB items. List/describe tables, get/put/update/delete items, query, scan, and batch operations. No admin (create/delete table) access.",
    "type": "stdio",
    "command": "npx",
    "args": ["-y", "aws-dynamodb-mcp-server@0.2.0"],
    "env": {
      "AWS_REGION": "us-east-1",
      "AWS_ACCESS_KEY_ID": "${AWS_ACCESS_KEY_ID}",
      "AWS_SECRET_ACCESS_KEY": "${AWS_SECRET_ACCESS_KEY}",
      "DYNAMODB_ENABLED_TOOL_GROUPS": "readonly,readwrite"
    }
  },
  "pulsemcp-s3-ro": {
    "title": "AWS S3 (Read-Only)",
    "description": "Use when you need to browse or read S3 data. List buckets and objects, download object contents, and check bucket existence. Read-only — no writes, deletes, or bucket creation.",
    "type": "stdio",
    "command": "npx",
    "args": [
      "-y",
      "s3-aws-mcp-server@0.0.1"
    ],
    "env": {
      "AWS_ACCESS_KEY_ID": "${AWS_ACCESS_KEY_ID}",
      "AWS_SECRET_ACCESS_KEY": "${AWS_SECRET_ACCESS_KEY}",
      "AWS_REGION": "us-east-1",
      "S3_ENABLED_TOOLGROUPS": "readonly"
    }
  },
  "pulsemcp-s3-rw": {
    "title": "AWS S3 (Read-Write)",
    "description": "Use when you need full S3 access. List, read, upload, copy, and delete objects. Create and delete buckets. Use S3_BUCKET to constrain to a single bucket.",
    "type": "stdio",
    "command": "npx",
    "args": [
      "-y",
      "s3-aws-mcp-server@0.0.1"
    ],
    "env": {
      "AWS_ACCESS_KEY_ID": "${AWS_ACCESS_KEY_ID}",
      "AWS_SECRET_ACCESS_KEY": "${AWS_SECRET_ACCESS_KEY}",
      "AWS_REGION": "us-east-1",
      "S3_ENABLED_TOOLGROUPS": "readonly,readwrite"
    }
  },
  "chrome-devtools-remote": {
    "title": "Chrome DevTools (Remote)",
    "description": "Use when you need to inspect or debug a page already open in Chrome. Connects to a running Chrome instance via --remote-debugging-port. Take screenshots, inspect network requests, read console logs, run scripts, and profile performance.",
    "type": "stdio",
    "command": "npx",
    "args": [
      "-y",
      "chrome-devtools-mcp@0.16.0",
      "--autoConnect",
      "--no-usage-statistics"
    ]
  },
  "chrome-devtools-headless": {
    "title": "Chrome DevTools (Headless)",
    "description": "Use when you need a standalone headless Chrome for automated browsing, screenshots, network analysis, and performance profiling. Launches its own Chrome instance with no visible window.",
    "type": "stdio",
    "command": "npx",
    "args": [
      "-y",
      "chrome-devtools-mcp@0.16.0",
      "--headless",
      "--no-usage-statistics"
    ]
  },
  "neon-remote-ro": {
    "title": "Neon Postgres (Remote, Read-Only)",
    "description": "Use when you need to safely inspect Neon Postgres databases. Run SQL queries, list projects and branches, check schemas, and analyze performance without risk of accidental modifications.",
    "type": "streamable-http",
    "url": "https://mcp.neon.tech/mcp",
    "headers": {
      "Authorization": "Bearer ${NEON_API_KEY}",
      "x-read-only": "true"
    }
  },
  "neon-remote-rw": {
    "title": "Neon Postgres (Remote, Read-Write)",
    "description": "Use when you need to manage Neon Postgres databases. Create and manage projects, branches, and databases, run SQL queries, and perform branch-based migrations.",
    "type": "streamable-http",
    "url": "https://mcp.neon.tech/mcp",
    "headers": {
      "Authorization": "Bearer ${NEON_API_KEY}"
    }
  }
}
